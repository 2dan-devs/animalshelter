'use strict';

describe('Service: SharedServices', function () {

  // load the service's module
  beforeEach(module('animalShelterApp'));

  // instantiate service
  var SharedServices;
  beforeEach(inject(function (_SharedServices_) {
    SharedServices = _SharedServices_;
  }));

  // it('should do something', function () {
  //
  // });

});
